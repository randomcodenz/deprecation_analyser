# frozen_string_literal: true

module RailsDeprecationLogAnalyser
  module Parser
    class NitraBuildlogParser
      # Should accept a bunch of options with reasonable defaults
      # - do we want to output the leftover lines to a file?
      # - do we want to output the "cleaned" dep log to a file?
    end
  end
end
